<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="quetion_form">
        <%= render 'shared/quetion_form' %>
      </section>
    </aside>
    <div>
<!--
      <h3>Micropst Feed</h3>
        <%= render 'shared/feed' %>
    </div>
-->
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>Welcome to the quetions</h1>
    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
